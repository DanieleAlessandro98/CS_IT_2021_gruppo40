/**
 * Package strutturale che si applica al pattern DAO
 * Questo è l'unico ad interfacciarsi in modo diretto con le classi del pattern DAO, e comunicare con essi
 * Offre per l'appunto i servizi cui il software ha bisogno, e cui le classi DAO forniscono
 */
package database.services;